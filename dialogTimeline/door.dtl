if {is_using_item} == true and {is_right_item} == true:
	Do you want to use the {item_name} on the door?
	- Use {item_name}.
		You use the key on the door and it opened.
	- Forget it.
else:
	player: I can't open this door, seems like it's looked.
	player: Maybe the dog could help me out.
